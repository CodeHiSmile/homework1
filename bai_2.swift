
import Foundation

func bai2() -> String {
    
    print("Nhap vao so nguyen duong: ")
    let number = Int(String(readLine() ?? "")) ?? 0
    
    if(String(number).count < 2){
        return String(number);
    } else{
        var numberMax: Int = 0;
        var numberMin: Int = 0;
        
        for i in 0...String(number).count{
            
        }
    }
    
    return "";
}
